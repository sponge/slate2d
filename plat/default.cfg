set vid.showfps 1
set vid.swapInterval 1

filewatcher_add "/"
set filewatcher.execute "js_reload"

bind p "toggle engine.pause"
bind o "frame_advance"
bind m "toggle snd.volume"

bind f1 toggle debug.assets

bind leftarrow +left
bind rightarrow +right
bind uparrow +up
bind downarrow +down
bind z +jump
bind x +shoot
bind enter +start

bind pad1_up +up
bind pad1_down +down
bind pad1_left +left
bind pad1_right +right
bind pad1_b +jump
bind pad1_a +shoot
bind pad1_start +start

bind pad2_up +up
bind pad2_down +down
bind pad2_left +left
bind pad2_right +right
bind pad2_b +jump
bind pad2_a +shoot
bind pad2_start +start

bind pad3_up +up
bind pad3_down +down
bind pad3_left +left
bind pad3_right +right
bind pad3_b +jump
bind pad3_a +shoot
bind pad3_start +start

bind pad4_up +up
bind pad4_down +down
bind pad4_left +left
bind pad4_right +right
bind pad4_b +jump
bind pad4_a +shoot
bind pad4_start +start