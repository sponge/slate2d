set vid.showfps 1
set vid.swapInterval 1

bind p "toggle engine.pause"
bind o "frame_advance"
bind m "toggle snd.volume"

bind f1 toggle debug.assets
bind f2 toggle debug.wrenInspector

bind leftarrow +left
bind rightarrow +right
bind uparrow +up
bind downarrow +down
bind z +b
bind x +a
bind a +y
bind s +x
bind d +l
bind c +r
bind enter +start
bind "'" +select

bind pad1_up +up
bind pad1_down +down
bind pad1_left +left
bind pad1_right +right
bind pad1_b +b
bind pad1_a +a
bind pad1_y +y
bind pad1_x +x
bind pad1_l1 +l
bind pad1_r1 +r
bind pad1_start +start
bind pad1_back +select

bind pad2_up +up
bind pad2_down +down
bind pad2_left +left
bind pad2_right +right
bind pad2_b +b
bind pad2_a +a
bind pad2_y +y
bind pad2_x +x
bind pad2_l1 +l
bind pad2_r1 +r
bind pad2_start +start
bind pad2_back +select

bind pad3_up +up
bind pad3_down +down
bind pad3_left +left
bind pad3_right +right
bind pad3_b +b
bind pad3_a +a
bind pad3_y +y
bind pad3_x +x
bind pad3_l1 +l
bind pad3_r1 +r
bind pad3_start +start
bind pad3_back +select

bind pad4_up +up
bind pad4_down +down
bind pad4_left +left
bind pad4_right +right
bind pad4_b +b
bind pad4_a +a
bind pad4_y +y
bind pad4_x +x
bind pad4_l1 +l
bind pad4_r1 +r
bind pad4_start +start
bind pad4_back +select