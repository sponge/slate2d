set vid.swapInterval 0
set vid.maxfps 120

bind leftarrow +left
bind rightarrow +right
bind uparrow +up
bind downarrow +down
bind Z +flap
bind X +flap
bind space +flap
bind enter +start

bind pad1_up +up
bind pad1_down +down
bind pad1_left +left
bind pad1_right +right
bind pad1_b +flap
bind pad1_a +flap
bind pad1_y +flap
bind pad1_x +flap
bind pad1_start +start

bind pad2_up +up
bind pad2_down +down
bind pad2_left +left
bind pad2_right +right
bind pad2_b +flap
bind pad2_a +flap
bind pad2_y +flap
bind pad2_x +flap
bind pad2_start +start

bind pad3_up +up
bind pad3_down +down
bind pad3_left +left
bind pad3_right +right
bind pad3_b +flap
bind pad3_a +flap
bind pad3_y +flap
bind pad3_x +flap
bind pad3_start +start

bind pad4_up +up
bind pad4_down +down
bind pad4_left +left
bind pad4_right +right
bind pad4_b +flap
bind pad4_a +flap
bind pad4_y +flap
bind pad4_x +flap
bind pad4_start +start