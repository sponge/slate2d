import * as SLT from 'slate2d';
import * as Draw from 'draw';

class Main {
  save() {
    return [];
  }

  constructor(initialState) {
      
  }

  update(dt) {
      
  }

  draw() {
    Draw.clear(0, 0, 0, 255);
    Draw.rect(128, 128, 64, 64, false);
  }
}

export default Main;